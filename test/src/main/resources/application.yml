server:
  port: 8080

spring.datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: test
    password: test

spring.jpa:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
    show-sql: true
    properties.hibernate.format_sql: true

spring:
  main:
    allow-bean-definition-overriding: true
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
      
springboot.jwt.secret: otipms!@#
